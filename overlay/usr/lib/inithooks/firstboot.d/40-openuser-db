#!/bin/bash -e
# regenerate odoo site salt and pgsql password

. /etc/default/inithooks

DBO_PASS=$(mcookie)
SITESALT=$(mcookie)$(mcookie)

$INITHOOKS_PATH/bin/pgsqlconf.py --user=openuser --pass="$DBO_PASS"
sed -i "s|db_password =.*|db_password = '$DBO_PASS';|" $CONF		
